services:
  - type: web
    name: watersync-ai-api
    runtime: python
    runtimeVersion: 3.11.8
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
